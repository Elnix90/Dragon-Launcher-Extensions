<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Permission Internet nécessaire pour vérifier GitHub -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    
    <!-- Permission pour Dragon Launcher -->
    <uses-permission android:name="org.elnix.dragonlauncher.APP_EXTENSION" />

    <application
        android:allowBackup="true"
        android:label="Dragon Auto-Update Extension"
        android:supportsRtl="true">

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="org.elnix.dragonlauncher.autoupdate.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <service
            android:name=".UpdateManagerService"
            android:enabled="true"
            android:exported="true"
            android:permission="org.elnix.dragonlauncher.APP_EXTENSION">
            <intent-filter>
                <action android:name="org.elnix.dragonlauncher.ACTION_CHECK_UPDATES" />
            </intent-filter>
        </service>

    </application>

</manifest>
