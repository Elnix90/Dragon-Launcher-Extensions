name: Build Extensions APKs

on:
    push:
        tags:
            - "v*"
    workflow_dispatch:

jobs:
    prepare-version:
        name: Prepare Version
        runs-on: ubuntu-latest
        outputs:
            new_tag: ${{ steps.bump.outputs.new_tag }}
        steps:
            - name: Get latest release
              id: get_latest
              uses: actions/github-script@v7
              with:
                  script: |
                      try {
                        const { data } = await github.rest.repos.getLatestRelease({
                          owner: context.repo.owner,
                          repo: context.repo.repo,
                        });
                        core.setOutput('tag', data.tag_name);
                      } catch (e) {
                        core.setOutput('tag', 'v0.0.0');
                      }
            - name: Calculate next version
              id: bump
              run: |
                  LATEST_TAG="${{ steps.get_latest.outputs.tag }}"
                  # Extraction du numéro (ex: v5 -> 5)
                  VERSION=${LATEST_TAG#v}
                  # Incrémentation simple (v1, v2, v3...)
                  NEW_V=$((VERSION + 1))
                  NEW_TAG="v${NEW_V}"
                  echo "new_tag=$NEW_TAG" >> $GITHUB_OUTPUT

    build:
        name: Build ${{ matrix.extension }}
        needs: prepare-version
        runs-on: ubuntu-latest
        permissions:
            contents: write
        strategy:
            fail-fast: false
            matrix:
                extension:
                    [
                        ext-internet-proxy,
                        ext-auto-update,
                        ext-shizuku-installer,
                        ext-additional-fonts,
                    ]

        steps:
            - name: Checkout
              uses: actions/checkout@v4

            - name: Set up JDK 17
              uses: actions/setup-java@v4
              with:
                  distribution: "temurin"
                  java-version: "17"
                  cache: "gradle"

            - name: Setup Gradle
              uses: gradle/actions/setup-gradle@v4
              with:
                  gradle-version: wrapper

            - name: Decode keystore
              env:
                  KEYSTORE_FILE_B64: ${{ secrets.KEYSTORE_FILE_B64 || '' }}
              run: |
                  if [ -n "$KEYSTORE_FILE_B64" ]; then
                    echo "$KEYSTORE_FILE_B64" | base64 -d > release.jks
                  else
                    echo "Warning: KEYSTORE_FILE_B64 secret is missing"
                  fi

            - name: Build Extension APK
              env:
                  KEYSTORE_FILE: ${{ github.workspace }}/release.jks
                  KEYSTORE_PASSWORD: ${{ secrets.KEYSTORE_PASSWORD || '' }}
                  KEY_ALIAS: ${{ secrets.KEY_ALIAS || '' }}
                  KEY_PASSWORD: ${{ secrets.KEY_PASSWORD || '' }}
              run: |
                  chmod +x gradlew
                  # Build only the specific module from the matrix
                  if [ -f "release.jks" ]; then
                    ./gradlew :${{ matrix.extension }}:assembleRelease
                  else
                    ./gradlew :${{ matrix.extension }}:assembleDebug
                  fi

            - name: Prepare APK for release
              run: |
                  mkdir -p release-artifacts
                  find ${{ matrix.extension }}/build/outputs/apk/ -name "*.apk" -exec cp {} release-artifacts/ \;

            - name: Create GitHub Release
              uses: softprops/action-gh-release@v2
              with:
                  files: release-artifacts/*.apk
                  body: "Official APKs for Dragon Launcher Extensions."
                  tag_name: ${{ github.ref_type == 'tag' && github.ref_name || needs.prepare-version.outputs.new_tag }}
                  append_body: true
                  generate_release_notes: true
                  prerelease: false
              env:
                  GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

            - name: Generate Badge Data
              if: always()
              run: |
                  mkdir -p badges
                  STATUS="${{ job.status == 'success' && 'Passed' || 'Failed' }}"
                  COLOR="${{ job.status == 'success' && 'green' || 'red' }}"
                  echo "{\"schemaVersion\": 1, \"label\": \"\", \"message\": \"$STATUS\", \"color\": \"$COLOR\", \"style\": \"for-the-badge\"}" > badges/${{ matrix.extension }}.json

            - name: Upload Badge Artifact
              uses: actions/upload-artifact@v4
              if: always()
              with:
                  name: badge-${{ matrix.extension }}
                  path: badges/${{ matrix.extension }}.json

    deploy-badges:
        needs: build
        runs-on: ubuntu-latest
        if: always()
        permissions:
            contents: write
        steps:
            - name: Checkout
              uses: actions/checkout@v4

            - name: Download All Badge Artifacts
              uses: actions/download-artifact@v4
              with:
                  path: all-badges
                  pattern: badge-*
                  merge-multiple: true

            - name: Deploy Badges to Branch
              uses: peaceiris/actions-gh-pages@v3
              with:
                  github_token: ${{ secrets.GITHUB_TOKEN }}
                  publish_dir: ./all-badges
                  publish_branch: badges
                  force_orphan: false
